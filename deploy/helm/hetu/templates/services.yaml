apiVersion: v1
kind: Service
metadata:
  name: {{ include "hetu.fullname" . }}-collector
  namespace: {{ .Release.Namespace }}
  labels:
    {{- include "hetu.labels" . | nindent 4 }}
    app.kubernetes.io/component: collector
spec:
  type: ClusterIP
  ports:
    - port: 8080
      targetPort: http
      protocol: TCP
      name: http
    - port: 9090
      targetPort: metrics
      protocol: TCP
      name: metrics
  selector:
    {{- include "hetu.selectorLabels" . | nindent 4 }}
    app.kubernetes.io/component: collector
---
apiVersion: v1
kind: Service
metadata:
  name: {{ include "hetu.fullname" . }}-analyzer
  namespace: {{ .Release.Namespace }}
  labels:
    {{- include "hetu.labels" . | nindent 4 }}
    app.kubernetes.io/component: analyzer
spec:
  type: ClusterIP
  ports:
    - port: 8081
      targetPort: http
      protocol: TCP
      name: http
    - port: 9091
      targetPort: metrics
      protocol: TCP
      name: metrics
  selector:
    {{- include "hetu.selectorLabels" . | nindent 4 }}
    app.kubernetes.io/component: analyzer
---
apiVersion: v1
kind: Service
metadata:
  name: {{ include "hetu.fullname" . }}-dashboard
  namespace: {{ .Release.Namespace }}
  labels:
    {{- include "hetu.labels" . | nindent 4 }}
    app.kubernetes.io/component: dashboard
spec:
  type: {{ .Values.dashboard.service.type | default "ClusterIP" }}
  ports:
    - port: 3000
      targetPort: http
      protocol: TCP
      name: http
      {{- if and (eq (.Values.dashboard.service.type | default "ClusterIP") "NodePort") .Values.dashboard.service.nodePort }}
      nodePort: {{ .Values.dashboard.service.nodePort }}
      {{- end }}
  selector:
    {{- include "hetu.selectorLabels" . | nindent 4 }}
    app.kubernetes.io/component: dashboard
---
{{- if .Values.chatbot.enabled }}
apiVersion: v1
kind: Service
metadata:
  name: {{ include "hetu.fullname" . }}-chatbot
  namespace: {{ .Release.Namespace }}
  labels:
    {{- include "hetu.labels" . | nindent 4 }}
    app.kubernetes.io/component: chatbot
spec:
  type: {{ .Values.chatbot.service.type | default "ClusterIP" }}
  ports:
    - port: 8000
      targetPort: http
      protocol: TCP
      name: http
      {{- if and (eq (.Values.chatbot.service.type | default "ClusterIP") "NodePort") .Values.chatbot.service.nodePort }}
      nodePort: {{ .Values.chatbot.service.nodePort }}
      {{- end }}
  selector:
    {{- include "hetu.selectorLabels" . | nindent 4 }}
    app.kubernetes.io/component: chatbot
{{- end }}
