apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: {{ include "cluster-intel.fullname" . }}-data
  labels:
    {{- include "cluster-intel.labels" . | nindent 4 }}
spec:
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 1Gi
